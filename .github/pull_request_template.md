## What changed

One or two sentences, plain language.

## Evidence

What was verified, the command that was run, and the output that proves it:

    $ the command
    the relevant lines of output

If CI already covers it, name the check. If something could not be verified, say so here rather than leaving it out.
